[package]
name = "application"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = "0.1.89"
domain = { version = "0.1.0", path = "../domain" }
serde = { version = "1.0.228", features = ["derive"] }
